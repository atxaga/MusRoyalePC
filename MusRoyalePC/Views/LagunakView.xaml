<UserControl x:Class="MusRoyalePC.Views.LagunakView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="950" d:DesignWidth="450">

    <UserControl.Resources>
        <SolidColorBrush x:Key="WoodTextureBg" Color="#8B6B4D"/>
        <SolidColorBrush x:Key="PanelCrema" Color="#F5E6CA"/>

        <DataTemplate x:Key="FriendItemTemplate">
            <Border Background="#FDF6E3" CornerRadius="28" Margin="0,0,0,14" Padding="12,18">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="4" ShadowDepth="1" Opacity="0.1"/>
                </Border.Effect>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Grid Margin="5,0,0,0">
                        <Ellipse Width="70" Height="70" Fill="#E6D2B1" Stroke="#C4A484" StrokeThickness="2"/>
                        <TextBlock Text="🛡️" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="32"/>
                    </Grid>

                    <StackPanel Grid.Column="1" Margin="18,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="{Binding Name}" FontWeight="Bold" Foreground="#4A342E" FontSize="22"/>
                        <TextBlock Text="{Binding Status}" Foreground="#8B7355" FontSize="15" Margin="0,2,0,0"/>
                    </StackPanel>

                    <Button Grid.Column="2" Content="{Binding PrimaryActionLabel}" 
                            Width="115" Height="48" Margin="0,0,5,0"
                            Background="#D9C5A3" Foreground="#4A342E"
                            BorderThickness="0" FontWeight="Bold" FontSize="16">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="16"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </Grid>
            </Border>
        </DataTemplate>

        <Style x:Key="BaseTabItem" TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Grid Name="Panel" Margin="0,0,0,10">
                            <StackPanel HorizontalAlignment="Center">
                                <ContentPresenter ContentSource="Header" Margin="15,5"/>
                                <Border Name="Line" Height="3" Width="40" Background="White" CornerRadius="1.5" Visibility="Collapsed" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Line" Property="Visibility" Value="Visible"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="False">
                                <Setter Property="Foreground" Value="#D7CCC8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource WoodTextureBg}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="30,40,30,10">
            <TextBlock Text="Lagunak" FontSize="52" FontWeight="Bold" Foreground="White"/>
            <TextBlock Text="Zure lagunak kudeatu eta bilatu" FontSize="19" Foreground="#D7CCC8" Margin="2,-5,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" Background="#22000000" CornerRadius="28" Height="58" Margin="25,20" Padding="20,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="SearchIcon" Text="🔍" VerticalAlignment="Center" Foreground="#BDBDBD" FontSize="20" Margin="0,0,15,0"/>

                <Grid Grid.Column="1">
                    <TextBox x:Name="SearchBox" Background="Transparent" BorderThickness="0" 
                             VerticalAlignment="Center" Foreground="White" FontSize="18" 
                             CaretBrush="White" VerticalContentAlignment="Center"/>

                    <TextBlock IsHitTestVisible="False" Text="Bilatu lagunak..." VerticalAlignment="Center" 
                               Foreground="#BDBDBD" FontSize="18" Margin="2,0,0,0">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>